(declare-const a Int)
(declare-const b Int)
(declare-const c Int)
(assert (not (or (or (<= a 0) (<= b 0)) (<= c 0))))
(assert (not (not (and (> (+ a b) c) (and(> (+ a c) b) (> (+ b c) a))))))
(assert (not (and(= a b) (= b c))))
(assert(or (= a b) (or (= b c) (= c a))))
(check-sat)
(get-model)